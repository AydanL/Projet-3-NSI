
import csv
def characters():
    with open("Characters.csv", mode='r', encoding='utf-8') as f:
        reader = csv.DictReader(f, delimiter=';')
        table_fusion = [{key : value for key, value in element.items()} for element in reader]
    
    with open("Caracteristiques_des_persos(1).csv", mode='r', encoding='utf-8') as f:
        reader = csv.DictReader(f, delimiter=';')
        table_fusion_2 = [{key : value for key, value in element.items()} for element in reader]
  
    for  in table_fusion:
        
        
    
        
        return table_fusion

print(characters()) 

        
